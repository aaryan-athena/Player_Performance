import { useAuth } from '../contexts/AuthContext.jsx';

/**
 * Re-export useAuth hook for convenient importing
 * This allows components to import useAuth directly from hooks/useAuth.js
 */
export { useAuth };
export default useAuth;